//! Tests for control-command helpers (`forward_skill_if_compound`,
//! `is_pure_control_cmd`, `plan_compound_for_submit`, `is_compound_plan_cmd`).
use super::*;

mod forward_skill;
mod is_pure_control;
mod plan_compound;
