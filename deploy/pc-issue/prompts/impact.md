Stage impact: trace the user's symptom from workspace global/current business indices into real source. Output outcome mapped or incomplete; source_baselines:[{repo,commit}], hypotheses:[{description,source,validation}], gaps:[], affected_paths:[], and reproduction:{case_source,case_ids,assertions}. Use an existing original case when applicable, preserving its prompts, turns and materials. If no suitable case exists, author a faithful frozen case and explicit assertions; do not replace the original symptom with an easier demo. Keep diagnostic screenshots separate from product input attachments. Verify baseline UI and installed app version during reproduction, not from Git alone. Retain the original source for every business expectation.
