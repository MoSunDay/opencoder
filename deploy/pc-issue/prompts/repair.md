Stage repair: independently read baseline evidence. If the issue did not reproduce and no evidence-backed change is justified, return not_needed with reason. If the cause is established, create a minimal fix in an isolated worktree. If extra telemetry is necessary, a diagnostic-only change is permitted and must be labeled as such. Output changed, not_needed or blocked. changed requires purpose=fix|diagnostic, worktree, changes:[{repo,baseline_commit,diff_path,diff_sha256}], build_request:{repo,branch,commit,worktree,purpose,original_assertions,changes} (changes must be the same verified diff list; repo identifiers must match), and evidence. Run appropriate source tests and preserve their real results. Never invent a commit for an uncommitted diff. Pin the worktree's actual content for the Team; do not allow it to build the branch head without the change. No database deletions or credential-state changes.
