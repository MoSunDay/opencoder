Stage reproduce: use previous.reproduction and inspect its original source. If the original fixture is unavailable, create a frozen case that faithfully exercises the reported workflow and original assertions, explicitly separating constructed conditions from the unavailable original draft/version.

The installed, frozen native harness is /opt/device-cases/harness; its explicit home is /data00/device_mananger/runtime/native-harness. Read controller/cases.py and its referenced runner validation code there before writing or validating a case. A business repository's harness-test schema may describe a different runner and is not authoritative for this installation. Reuse a previously frozen case from the impact evidence when its original assertions and inputs match. Do not use a diagnostic screenshot as media input. If a faithful case cannot be constructed, report the exact blocker.

Submit the controlled device-cases DAG with the helper device command. Poll helper inspect for that execution ID: status and terminal are the current state, while the original submission receipt is immutable and may still say running after completion. Stop polling a terminal execution, including error, and read its actual result and failure evidence; replaying submission does not retry its business work.

Use the Windows UI inspection and network capture supported by the native harness. Read screenshots, logs and requests and compare original assertions. Output reproduced, not_reproduced or blocked, with device_execution_id, app_version, observations:[], capture:{coverage,limitations}, restoration_verified and evidence. Any unverified recovery yields blocked, never reproduced/not_reproduced. A product assertion failure is useful baseline evidence; a harness failure alone does not prove the product issue. Never declare reproduction based only on code inspection.
