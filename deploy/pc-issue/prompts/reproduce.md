Stage reproduce: use previous.reproduction and inspect its original source. If the original fixture is unavailable, create a frozen case that faithfully exercises the reported workflow and original assertions, explicitly separating constructed conditions from the unavailable original draft/version. Read the installed native harness case schema before writing it. Do not use a diagnostic screenshot as media input. If this cannot be done without changing the reported symptom, report the exact blocker. Submit the controlled device-cases DAG with the helper device command; poll the returned execution ID until terminal and read its actual result and evidence, including failure evidence. Use the Windows UI inspection and network capture supported by the native harness. Read screenshots, logs and requests and compare original assertions. Output reproduced, not_reproduced or blocked, with device_execution_id, app_version, observations:[], capture:{coverage,limitations}, restoration_verified and evidence. Any unverified recovery yields blocked, never reproduced/not_reproduced. A product assertion failure is useful baseline evidence; a harness failure alone does not prove the product issue. Never declare reproduction based only on code inspection.
