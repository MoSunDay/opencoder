Stage verify: if repair was not_needed or blocked, propagate not_needed or blocked with the actual reason; do not invoke jy-builder. Otherwise use the helper build command to submit the frozen build_request to jy-builder. Independently inspect/download artifacts and verify source and hashes before installation. Use the controlled device path for candidate installation/selection and rerun exactly the original reproduction and impacted assertions. Do not substitute an old installed app for the candidate. For diagnostic builds, use evidence to revise the hypothesis/fix within the two-round bound. Output verified, failed or blocked with build_execution_id, device_execution_id, artifacts:[], assertions:[{name,passed,evidence}], download_verified, restoration_verified, purpose, rounds and evidence. verified requires every original assertion passing, verified download and recovery; diagnostic success is not a fixed product.

Prepare a candidate manifest with archive_path (portable ZIP), archive_sha256, archive_bytes, download_sha256, build_execution_id, commit, app_relative_path, app_sha256, package_relative_path and source. Submit device --stage verify --candidate MANIFEST. The controlled candidate adapter installs the verified bytes in an isolated directory and uses a per-work profile; original app restoration remains owned by the native supervisor. Verify the actual native/Host provenance and original assertions from that execution.
