# 大脑恢复运行后的当前错误状态

模型服务暂时不可用导致调度阻塞后，通过暂停、继续恢复同一运行时，旧错误仍留在当前运行状态中，页面会继续显示“运行阻塞或失败”。

成功继续调度时清除当前错误，保留原运行 ID、轮次、能力执行 ID 和历史阻塞事件。运行面板只在当前状态为阻塞或失败时展示错误提示；旧版本已经恢复的记录也按当前状态展示，原诊断仍可从事件历史查看。

回归覆盖完成上一轮后的阻塞、暂停、恢复，以及页面从阻塞到等待执行和完成的状态切换；同时核对执行身份保持和历史诊断可读。
