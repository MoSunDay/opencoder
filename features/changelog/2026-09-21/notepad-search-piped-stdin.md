# 笔记搜索固定使用工作目录

笔记搜索在继承管道输入时，`rg` 会搜索标准输入，导致工作目录中的文件没有匹配结果。现在显式传入搜索目录，并在 `rg` 和 `grep` 的查询前结束选项解析；以 `-` 开头的查询也作为搜索内容处理。

同一 TUI 测试程序使用空设备输入通过、使用空管道输入失败，复现了原问题。修正后的实际搜索命令在这两种输入方式下均能找到文件，普通查询和 `--version` 查询共四组检查通过；新增前导连字符查询的回归测试。

完整工作区测试及发布结果归入最终候选验收回执。复现与命令验证：`/var/tmp/brain-plan-simplification-c7d8090f/tui-search-stdin-command-verification.json`。
