# DAG 取消后的子进程清理

控制进程在取消宽限期内写完清理回执并退出后，后台子进程仍可能持有 stdout/stderr，导致执行器等待输出读取线程。现在在确认控制进程退出后清理残留进程组，保留取消状态与已落盘回执。

`op_cancellation_grace_allows_durable_cleanup_receipt` 增加后台进程持管道场景，核验回执内容及取消返回时间。移除进程组清理时回归失败；恢复修复后通过。

未开始步骤因整次运行取消而结束时，继续写入既定 `run cancelled` 错误文本；运行中取消端到端回归核验终态、步骤文件和文本合同。
