Commit: c23acdf9647a182a91323db26fb7e46191ac5f19

# 大脑与 PC 诊断计划生效

里程碑 schema 6 工作台与按轮执行明细已发布。已有 `pc-issue` v1/v2 保留为历史 schema 5；安装入口追加 v3 并在重复调用时保持幂等，工作台从最新版本启动。

能力目录中的动态定义若缺少实例输入而无法解析，只将对应能力标为不可用并展示原因；画布禁止新挂载，计划引用时直接返回错误，不阻断其他计划。

发布采用可追溯制品 `c23acdf9`。生产信号切换完成 900 秒观察，171 个采样通过；原有任务持续运行，公开就绪探针无失败。PC v3 的线上创建、旧版保留和重复安装已核验。
